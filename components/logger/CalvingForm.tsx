// CalvingForm — Form to log a calving event (calf sex, alive/dead, assisted/unassisted)
export default function CalvingForm() {
  return <div className="p-4 text-stone-400 text-sm">[CalvingForm] — not yet implemented</div>;
}
